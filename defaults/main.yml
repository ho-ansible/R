---
R_cran_mirror: "http://cloud.r-project.org/"
R_cran_ver: cran40
R_apt_packages:
  - libxml2-dev
  - libssl-dev
  - libcurl4-openssl-dev
R_packages: 
  - future
  - tidyverse
R_github_packages:

# First entry on .libPaths() in Debian, trailing slash
R_libloc: /usr/local/lib/R/site-library/
